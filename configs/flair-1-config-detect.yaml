output_path: "../detect/"

zone:
  tile_factor: 2
  margin: 256
  output_dalle_size: 2048
  layers:
    - path: "../D085_2019-Z9_NF-ortho_img.tif"
      key: "rvbie"
      name: "rvbie"
      bands: [1, 2, 3, 4, 5]

img_pixels_detection: 512
output_type: "argmax"
n_classes: 19

model_weights: "../FLAIR-HF_15_scaling_RVBIE_unetresnet34.pth"
batch_size: 2
use_gpu: true
model_name: 'unet'
encoder_name: 'resnet34'
num_worker: 0  ### IF OS=WINDOWS ; set as 0

norm: scaling
norm_means:
norm_stds:
